/**
 * Imports
 */
import  Express  from "express";
const routerDisconnectNode = Express.Router();

/**
 * Main logic to disconnect node from network
 */
routerDisconnectNode.post("/", (req, res)=>{
        
})

/**
 * Export route logic to ../routes-index.ts
 */
export = routerDisconnectNode;